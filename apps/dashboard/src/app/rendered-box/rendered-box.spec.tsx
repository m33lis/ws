import React from 'react';
import { render } from '@testing-library/react';

import RenderedBox from './rendered-box';

describe('RenderedBox', () => {
  it('should render successfully', () => {
  });
});
